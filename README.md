# Rust / WASM / WebGl experiment

This repo is designed to work together with github-codespaces.

Build works now with parcel (Both Rust and Typescript), just run `npm run dev` or `npm build` in frontend directory.
